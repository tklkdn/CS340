<h1>{{tableTitle}}</h1>
	<p>This page shows the contents of the selected parent table.</p>
	<p>Sample contents of selected table:</p>
<table border="1">
  <tbody>
    <tr>
	  {{#each colList}}
        <th>{{this}}</th>
	  {{/each}}
	  <th></th>
	  <th></th>
    </tr>
	{{#each dataList}}
      <tr>
        {{#each this}}
		  <td>{{this}}</td>
		{{/each}}
	    <td><a href="/update?table={{../table}}">Edit</a></td>
	    <td><a href="#" onclick="confirmDel()">Delete</a></td>
	  </tr>
	{{/each}}
  </tbody>
</table>
<p><a href='/add?table={{table}}'>Add Row</a></p>

<script>
  var confirmDel = () => {
    if (confirm("Are you sure you would like to delete this item?")) {
	  console.log("Item deleted");
	} else {
	  console.log("Item not deleted");
	}
  };
</script>	